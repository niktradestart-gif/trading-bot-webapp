name: Update Trading Data

on:
  schedule:
    - cron: '*/2 * * * *'  # Каждые 2 минуты
  workflow_dispatch:       # Ручной запуск
  push:
    paths:
      - '**.js'           # Запуск при изменении JS файлов
      - '**.html'         # Запуск при изменении HTML файлов

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests
        
    - name: Update trading data
      env:
        BOT_API_URL: "http://89.23.98.206:8080"
      run: |
        python .github/scripts/update_data.py
        
    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add system_status.json last_signal.json last_result.json
        git diff --staged --quiet || git commit -m "🤖 Auto-update trading data [skip ci]"
        git push
